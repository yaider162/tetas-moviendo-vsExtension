// Constantes para comandos
export const COMMANDS = {
    INCREMENT_COUNTER: 'incrementar_contador',
    RESET_COUNTER: 'resetear_contador',
    SHOW_DATE: 'prueba'
} as const;

// Configuracion de la barra de estado
export const STATUS_BAR_CONFIG = {
    ALIGNMENT: 'left' as const,
    PRIORITY: 100
} as const;
